apiVersion: v1
kind: Service
metadata:
  name: app-docker-kubernetes-service
spec:
  type: NodePort
  selector:
    app: app-docker-kubernetes
  ports:
    - port: 3000
      targetPort: 3000
      nodePort: 30080